{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://seedance.local/contracts/makeup_plan.json",
  "title": "MakeupPlanFromLLM",
  "type": "object",
  "required": ["items"],
  "properties": {
    "items": {
      "type": "array",
      "minItems": 1,
      "maxItems": 6,
      "items": {
        "type": "object",
        "required": ["character_key", "prompt_en"],
        "properties": {
          "character_key": { "type": "string" },
          "prompt_en": { "type": "string" }
        },
        "additionalProperties": true
      }
    }
  },
  "additionalProperties": true
}
